<Page
    x:Class="VoatHub.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoatHub"
    xmlns:voatModels="using:VoatHub.Models.Voat.v1"
    xmlns:voatHubModels="using:VoatHub.Models.VoatHub"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <DataTemplate x:Key="SubmissionItemTemplate" x:DataType="voatModels:ApiSubmission">
            <Grid Margin="12,11,12,13">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind Title}"
                           Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                           Style="{ThemeResource BodyTextBlockStyle}" />

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Bind UserName}" MaxLines="1"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           Style="{ThemeResource BodyTextBlockStyle}" />

                    <TextBlock Text="{x:Bind Date}" Grid.Column="1" Grid.Row="1" Margin="12,2,0,0"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           Style="{ThemeResource BodyTextBlockStyle}" />
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="CommentItemTemplate" x:DataType="voatModels:ApiComment">
            <TextBlock Text="{x:Bind Content}"
                       Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                       HorizontalAlignment="Center"
                       Style="{ThemeResource BodyTextBlockStyle}" />
        </DataTemplate>

        <DataTemplate x:Key="CommentSubmissionTemplate" x:DataType="voatHubModels:CommentSubmission">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,8" Style="{ThemeResource TitleTextBlockStyle}"
                           HorizontalAlignment="Left" Text="{x:Bind Submission.Title}"/>

                    <TextBlock Margin="0,9" HorizontalAlignment="Left" MaxWidth="560"
                           Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind Submission.Content}" />
                    <ListView x:Name="CommentListView" 
                          ItemTemplate="{StaticResource CommentItemTemplate}"
                          ItemsSource="{x:Bind Comments}"
                          IsItemClickEnabled="True">
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate x:Key="LinkSubmissionTemplate" x:DataType="voatModels:ApiSubmission">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Margin="0,8" Style="{ThemeResource TitleTextBlockStyle}"
                           HorizontalAlignment="Left" Text="{x:Bind Title}"/>

                <WebView x:Name="submissionWebView" Source="{x:Bind Url}"
                         Grid.Row="1"></WebView>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MasterColumn" Width="400" />
            <ColumnDefinition x:Name="DetailColumn" Width="*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="MasterColumnGrid" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="MasterTopBar" 
                       Margin="12,0"
                       Text="Top bar" 
                       Style="{ThemeResource TitleTextBlockStyle}" />

            <ListView x:Name="MasterListView"  Grid.Row="1"
                ItemTemplate="{StaticResource SubmissionItemTemplate}"
                IsItemClickEnabled="True"
                ItemClick="MasterListView_ItemClick">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <CommandBar x:Name="MasterCommandBar" Grid.Row="2">
                <CommandBar.Content>
                    <TextBlock Text="Sort by: Hot" Margin="12,14"/>
                </CommandBar.Content>

                <AppBarButton Icon="Back" Label="Back" Tag="Back" Click="MasterCommandBarButton_Click"/>
                <AppBarButton Icon="Sort" Label="Sort">
                    <AppBarButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Hot" Tag="hot" Click="SortItem_Click"/>
                            <MenuFlyoutItem Text="New" Tag="new" Click="SortItem_Click"/>
                            <MenuFlyoutItem Text="Top" Tag="top" Click="SortItem_Click"/>
                        </MenuFlyout>
                    </AppBarButton.Flyout>
                </AppBarButton>

                <AppBarButton Icon="Refresh" Label="Refresh" Tag="refresh" Click="MasterCommandBarButton_Click"/>

                <!--<CommandBar.SecondaryCommands>
                    <AppBarButton Icon="Like" Label="Like" Click="AppBarButton_Click"/>
                    <AppBarButton Icon="Dislike" Label="Dislike" Click="AppBarButton_Click"/>
                </CommandBar.SecondaryCommands>-->
            </CommandBar>
        </Grid>

        <Grid x:Name="DetailColumnGrid" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ContentPresenter
                x:Name="DetailContentPresenter"
                Grid.Row="0"
                BorderThickness="1,0,0,0"
                Padding="24,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                DataContextChanged="DetailContentPresenter_DataContextChanged">
            </ContentPresenter>

            <CommandBar x:Name="DetailCommandBar" Grid.Row="1">
                <CommandBar.Content>
                    <TextBlock Text="Some space." Margin="12,14"/>
                </CommandBar.Content>
                
                <AppBarButton Icon="Back" Label="Back" Tag="back" Click="DetailCommandBarButton_Click"/>
                <AppBarButton Icon="Forward" Label="Forward" Tag="forward" Click="DetailCommandBarButton_Click"/>
                
                <AppBarSeparator/>

                <AppBarButton Icon="Refresh" Label="Refresh" Tag="refresh" Click="DetailCommandBarButton_Click"/>
                <AppBarButton Icon="Save" Label="Save" Tag="save" Click="DetailCommandBarButton_Click"/>
                <AppBarButton Icon="ReShare"  Label="Share" Tag="share" Click="DetailCommandBarButton_Click"/>

                <!--<CommandBar.SecondaryCommands>
                    <AppBarButton Icon="Dislike" Label="Dislike" Tag="dislike" Click="DetailCommandBarButton_Click"/>
                </CommandBar.SecondaryCommands>-->
            </CommandBar>
        </Grid>

        <!--DataTemplate is favored over programmatically changing values-->
        <!--<Grid Grid.Column="1" 
              BorderThickness="1,0,0,0" Padding="24,0" 
              HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
              BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="SubmissionTitleTextBlock" 
                       Margin="0,8" Grid.Row="0" 
                       Style="{ThemeResource TitleTextBlockStyle}"
                       HorizontalAlignment="Left"/>

            <WebView x:Name="SubmissionWebView" Grid.Row="1"
                     VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            </WebView>

            <TextBlock x:Name="SubmissionContentTextBlock" Grid.Row="2" 
                       Margin="0,8"  
                       Style="{ThemeResource TitleTextBlockStyle}" 
                       HorizontalAlignment="Left"/>
        </Grid>-->
    </Grid>
</Page>
